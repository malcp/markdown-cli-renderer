{
  "name": "@malcp/markdown-mume-renderer",
  "version": "0.1.0",
  "description": "Markdown renderer that uses mume",
  "bin": {
    "markdown-mume-renderer": "dist/index.js"
  },
  "scripts": {
    "build": "babel --plugins transform-runtime  src -d dist",
    "start": "node dist/index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marc Alcalà Prieto <marc.alcala@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@shd101wyy/mume": "^0.1.3",
    "commander": "^2.11.0",
    "fs-extra": "^4.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.6.0"
  }
}
